import { motion, useScroll, useTransform } from "framer-motion";
import { ArrowRight, ArrowDown } from "lucide-react";
import { GbnzLogo } from "./Logo";

const container = {
  hidden: {},
  show: { transition: { staggerChildren: 0.15 } },
};
const item = {
  hidden: { opacity: 0, y: 30 },
  show: { opacity: 1, y: 0, transition: { duration: 0.7, ease: [0.22, 1, 0.36, 1] as const } },
};

export function Hero() {
  const { scrollY } = useScroll(); 
  const blobY = useTransform(scrollY, [0, 600], [0, 150]);

  return (
    <section id="accueil" className="relative min-h-screen flex items-center pt-32 pb-20 overflow-hidden bg-white">
      <motion.div
        style={{ y: blobY }}
        className="absolute -top-32 -right-32 w-[500px] h-[500px] rounded-full bg-brand-yellow opacity-40 blur-3xl pointer-events-none"
      />
      <svg className="absolute bottom-10 left-0 w-64 opacity-30 pointer-events-none" viewBox="0 0 200 100">
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={i} x1={i * 20} y1="100" x2={i * 20 + 60} y2="0" stroke="#F5C400" strokeWidth="1.5" />
        ))}
      </svg>

      <div className="max-w-7xl mx-auto px-6 grid lg:grid-cols-5 gap-12 items-center relative z-10">
        <motion.div variants={container} initial="hidden" animate="show" className="lg:col-span-3">
          <motion.span variants={item} 
          className="inline-block px-4 py-1.5 rounded-full bg-brand-yellow text-brand-black text-xs font-semibold tracking-wide uppercase"
          >
            Studio Créatif · Kinshasa, RDC
          </motion.span>

          <motion.h1 variants={item} className="mt-6 font-display text-[48px] sm:text-[72px] lg:text-[96px] leading-[0.95] text-brand-black">
            Votre Vision,<br />
            <span className="text-brand-yellow">Notre Design.</span>
          </motion.h1>

          <motion.p variants={item} className="mt-6 text-lg text-brand-gray max-w-xl leading-relaxed">
            Gbnz Design transforme vos idées en identités visuelles percutantes — logos, affiches et branding sur mesure pour les entreprises ambitieuses.
          </motion.p>

          <motion.div variants={item} className="mt-8 flex flex-wrap gap-4">
            <motion.a
              href="#contact"
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.97 }}
              className="inline-flex items-center gap-2 px-7 py-4 rounded-full bg-brand-black text-white font-semibold"
            >
              Démarrer un projet <ArrowRight size={18} />
            </motion.a>
            <motion.a
              href="#portfolio"
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.97 }}
              className="inline-flex items-center gap-2 px-7 py-4 rounded-full border-2 border-brand-black text-brand-black font-semibold bg-transparent"
            >
              Voir nos créations
            </motion.a>
          </motion.div>

          <motion.div variants={item} className="mt-8 flex flex-wrap gap-6 text-sm text-brand-gray">
            <span><span className="text-brand-yellow">✦</span> +50 clients satisfaits</span>
            <span><span className="text-brand-yellow">✦</span> Kinshasa & International</span>
          </motion.div>
        </motion.div>

        <div className="lg:col-span-2 relative h-[420px] hidden lg:block">
          {[
            { rotate: -8, x: -20, y: 20, bg: "bg-brand-yellow", delay: 0.3 },
            { rotate: 4, x: 40, y: -10, bg: "bg-brand-black", delay: 0.5 },
            { rotate: -3, x: 0, y: 60, bg: "bg-white border-2 border-brand-black", delay: 0.7 },
          ].map((c, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0, y: 60, rotate: 0 }}
              animate={{ opacity: 1, y: c.y, rotate: c.rotate, x: c.x }}
              transition={{ delay: c.delay, type: "spring", stiffness: 80, damping: 14 }}
              className={`absolute inset-0 mx-auto w-72 h-96 rounded-2xl shadow-2xl ${c.bg} flex items-center justify-center`}
              style={{ zIndex: 3 - i }}
            >
              <GbnzLogo inverted={i === 0 || i === 2} />
            </motion.div>
          ))}
        </div>
      </div>

      <motion.div
        animate={{ y: [0, 10, 0] }}
        transition={{ repeat: Infinity, duration: 1.8 }}
        className="absolute bottom-8 left-1/2 -translate-x-1/2 text-brand-gray"
      >
        <ArrowDown size={24} />
      </motion.div>
    </section>
  );
}
